#exportPattern("^[[:alpha:]]+")
#Generated by codetoolsBioC version 0.0.16
#Timestamp: Wed Apr 25 11:48:40 2012

#Imports: GenomicRanges, IRanges, methods, Rsamtools, ShortRead, stats,
#         utils

#importMethodsFrom(GenomicRanges, cigar, seqnames, strand)

importMethodsFrom(IRanges, as.data.frame, as.matrix, cbind, colnames,
                  "colnames<-", countOverlaps, coverage, duplicated,
                  end, findOverlaps, gaps, match, na.omit, ncol,
                  nrow, paste, ranges, reduce, setdiff, sort, split,
                  splitRanges, start, sub, t, tail, tapply, unique,
                  unlist, width)

importMethodsFrom(GenomicRanges, cigar, seqnames, strand)

importMethodsFrom(Rsamtools, readBamGappedAlignments)

importMethodsFrom(ShortRead, lapply)

importFrom(GenomicRanges, GRanges, GRangesList)

importFrom(IRanges, IRanges)

#importFrom(methods, "@<-")

importFrom(stats, na.action)

importFrom(utils, read.table, write.table)

export(QuanBam, QuanGR, read.bed, read.GTF, read.GenePred, uiGR, diffGR)
